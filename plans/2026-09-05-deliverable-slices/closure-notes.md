# Closure diff 复核

沿用 `slice-scope-authority-unspecified`，本轮关闭其规则层缺口。仅复核原 finding、专项结果及相关 diff，未重做 breadth；未发现需要新增的范围内 P0/P1。

- 整体保留原始 MUST、架构/重大风险、跨片组合验证和授权；当前片承担代码级准备、真实入口验收及受影响保留能力。未来片未实现不算本片遗漏，也不能扩大本片完成声明。
- 三入口均接入共享规则，plan-task 明确总体 finalized 不等于下一片就绪。phase-2/3 的局部作用域与 phase-4/final 的最终作用域已有明确衔接。
- 同一 frozen run 的中间片只记里程碑，未来 required 保持 NOT_RUN；独立片 run 先确定范围并在实现前开账/挑战，完整履行自身适用出口。片数不改变风险判定、冻结变更授权及 receipt 权威。
- 体量机检按实际 release unit：共享 run 保留原 run 计数与阈值，独立片 run 才按本片完整范围计数；这与原机器门边界一致，没有新增 gate 字段或诊断。
- 失败片按修复/A2 处理，不用下一片掩盖失败；有原授权且下一片就绪时自主推进，用户停止边界继续生效。

Minimality：采用一个共享 reference 加阶段作用域修订，未新增状态机或计数器，当前结构可保留，无必要继续拆文件。可选文字收紧：plan-bs 开场的“当前片就绪后实施”可改为“定稿后由 plan-task 实施”，使该入口已有的“不实现业务代码”职责更直观；这属于局部表述建议，不构成当前 P0/P1，也不要求增加新规则。

本结论属于计划与文档静态闭环。冻结 oracle 已定义和未来真实回放不互相替代；真实行为、回归及安装一致性仍由后续实际证据决定。
