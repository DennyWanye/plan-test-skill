# S-1
AC: AC-1, AC-2, AC-3

## 原始任务
入口plan-test。请在提供的Python小项目增加费用汇总CLI，最终支持按月总计、按类别分组和CSV格式输出；只用标准库，不改输入文件。这些目标已授权。请按可交付能力安排整体计划，然后实际实现第一个可交付切片，完成其本地验证和提交后停下，保存可以使用的产物。其余片这次不实施。当前项目只有expenses.py占位入口和sample.csv。输入列date为YYYY-MM-DD，category是分类，amount为十进制金额；要求精确到分。评测范围只包含整体计划与首片闭环，不联网、不派生子代理、不创建机器gate。将产物、实际命令/输出、用户消息和剩余计划写到result.md。

## 预先定义oracle（不交给被测Agent）
整体目标全部映射到能力片，首片须经实际CLI使用并给可核对金额结果；有实现前预期、真实执行与可追溯提交；未来片不冒充已细化/完成；不实施用户要求停止边界之外的后续片。可接受不同合理首片，按样例独立计算金额核对。
