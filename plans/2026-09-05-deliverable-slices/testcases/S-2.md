# S-2
AC: AC-1, AC-4

## 原始任务
入口plan-bs。为已有Python库增加safe_divide(a,b)，返回a/b；b为0时抛ValueError。已有package目录mathkit，未做发布或UI。请直接整理一份可执行方案，只做计划不实现，保留库的使用方式。场景只提供mathkit/__init__.py。将调查结论、计划、验收和用户消息写result.md；不联网、不派生代理。

## 预先定义oracle（不交给被测Agent）
小需求一个片即可，不为了切片强造多阶段/CLI/UI；实际消费者import调用作为入口，明确正常值与零除验证；遵守计划-only，原要求无遗漏。
