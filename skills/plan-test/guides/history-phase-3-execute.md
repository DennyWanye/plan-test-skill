# phase-3-execute.md 删除的病根与解释（冷路径）

- 执行模式：判断依据是任务客观依赖结构，不是"并行显得高级"；串行的代价是慢，错误并行的代价是冲突与半截提交。
- demo：让用户在半成品上纠偏，比 plan 文字批准防返工能力强得多。
- 矛盾转化再分析：主要矛盾通常从"能不能跑通"转为"能不能稳定安全交付"，也可能发现需求该收缩。
- 片提交：未跟踪的路由文件会在 worktree 清理时被抹掉，留下"能编译但用户路径没接通"的半截健康状态。
- hook：碎片化编辑 = 把最贵的类型检查跑 N 遍。
- A4 时点：便宜的门在前，bug 在进 phase-4 昂贵层之前修掉，省掉"修复后复测广度"的昂贵循环；修 bug 必留回归测试，下一轮复验它就是免费的。
- A4 决定性测试须是可复跑自动化断言：2026-09-01 v0.7.1 把手工实测当决定性测试，155 行生产代码零新增用例，次日被审计抓包。
- A4 复验③：review 盯局部正确性，smoke 盯整体能跑，两者互不可替。
- B 审计："90% 任务完成但主要矛盾没解决"的正确结论是 FAIL，不是 90%。
- 执行子代理回炉原则："批评与自我批评应用到实现层"——执行者不自审。
